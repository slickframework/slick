build_settings:
    ignore:
        - "vendor"
        - "tests"
setup:
    composer:
        action: "install"
test:
	php_mess_detector:
        allow_failures: true
    php_code_sniffer:
        standard: "ZEND"
    php_cpd:
        allow_failures: true
